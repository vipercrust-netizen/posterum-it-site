import {redirect} from "next/navigation";import {getSession} from "@/lib/auth";import {loginAction} from "../actions";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){if(await getSession())redirect("/admin");const q=await searchParams;return <div className="loginBox"><div className="adminLogo">POSTERUM<span>IT</span></div><h1>Администрирование</h1><p>Войдите для работы с заявками.</p><form action={loginAction}><label>Логин<input name="login" autoComplete="username" required/></label><label>Пароль<input name="password" type="password" autoComplete="current-password" required/></label>{q.error&&<div className="adminError">Неверный логин или пароль.</div>}{q.changed&&<div className="adminOk">Данные изменены. Войдите снова.</div>}<button className="btn">Войти</button></form></div>}
