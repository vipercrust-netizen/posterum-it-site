"use server";import {redirect} from "next/navigation";import {revalidatePath} from "next/cache";import {getSession} from "@/lib/auth";import {saveSettings} from "@/lib/settings";
export async function saveSiteSettings(fd:FormData){if(!(await getSession()))redirect("/admin/login");const data={phone:String(fd.get("phone")||"").trim(),email:String(fd.get("email")||"").trim(),address:String(fd.get("address")||"").trim(),maxChatId:String(fd.get("maxChatId")||"").trim()};if(!data.phone||!data.email)redirect("/admin/settings?error=site");await saveSettings(data);revalidatePath("/admin/settings");revalidatePath("/contacts");redirect("/admin/settings?saved=1")}
