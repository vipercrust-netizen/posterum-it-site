import {redirect} from "next/navigation";import {getSession} from "@/lib/auth";import {changeCredentials} from "../actions";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const s=await getSession();if(!s)redirect("/admin/login");const q=await searchParams;return <><div className="adminTop"><div><h1>Настройки</h1><p>Доступ к административной панели.</p></div></div><section className="settingsCard"><h2>Сменить логин и пароль</h2><p>После сохранения потребуется войти заново.</p><form action={changeCredentials}><label>Новый логин<input name="login" defaultValue={s.login} required minLength={3}/></label><label>Новый пароль<input name="password" type="password" required minLength={8}/></label>{q.error&&<div className="adminError">{q.error==="login"?"Такой логин уже существует.":"Логин — от 3 символов, пароль — от 8."}</div>}<button className="btn">Сохранить</button></form></section><section className="settingsCard"><h2>MAX</h2><p>Токен бота и ID чата задаются на сервере через <code>MAX_BOT_TOKEN</code> и <code>MAX_CHAT_ID</code>. Секреты намеренно не показываются в браузере и не хранятся в GitHub.</p></section></>}
