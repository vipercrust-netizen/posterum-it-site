"use client";import {useState} from "react";
export default function CallbackForm({topic=""}:{topic?:string}){const [state,setState]=useState<"idle"|"sending"|"done"|"error">("idle");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const fd=new FormData(e.currentTarget);const body=Object.fromEntries(fd.entries());body.page=location.pathname;const r=await fetch("/api/leads",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});setState(r.ok?"done":"error");if(r.ok)e.currentTarget.reset()}return <form className="leadform" onSubmit={submit}><input name="name" placeholder="Ваше имя" required/><input name="phone" placeholder="+7 (___) ___-__-__" required/><input type="hidden" name="topic" value={topic}/><textarea name="comment" placeholder="Коротко опишите задачу"/><button className="btn" disabled={state==="sending"}>{state==="sending"?"Отправляем…":"Получить консультацию"}</button><small>Нажимая кнопку, вы соглашаетесь на обработку персональных данных.</small>{state==="done"&&<b className="success">Заявка отправлена. Мы свяжемся с вами.</b>}{state==="error"&&<b>Не удалось отправить. Позвоните нам: +7 (927) 328-70-29.</b>}</form>}
